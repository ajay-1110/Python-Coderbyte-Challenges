def iss_prime(num):
    return num>1 and all(num%i != 0 for i in range(2, int(num**0.5)+1))

def check(num):
    if iss_prime(num):
        return 'true'
    else:
        return 'false'
    
print(check(1008))
